apiVersion: kelos.dev/v1alpha2
kind: Task
metadata:
  name: review-pr
spec:
  type: claude-code
  prompt: "Review the latest PR and provide feedback using /review."
  credentials:
    type: oauth
    secretRef:
      name: claude-oauth-token
  workspaceRef:
    name: my-workspace
  # agentConfigRefs inject the AgentConfig's instructions and plugins
  # into this Task's agent container at runtime.
  agentConfigRefs:
    - name: code-reviewer
